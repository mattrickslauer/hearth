// Optional Alibaba SDKs — only imported (dynamically, guarded) when deploying
// against real cloud resources. No bundled types; we don't rely on their shapes.
declare module 'tablestore';
declare module 'ali-oss';
